apiVersion: v1
kind: Service
metadata:
  name: imgproxy
  labels:
    app: imgproxy
spec:
  type: LoadBalancer
  ports:
    - name: web
      port: 80
      targetPort: 80
  selector:
    app: imgproxy
